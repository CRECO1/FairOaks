# Drop the broken form here

Upload the PDF that rendered as empty boxes (the one Brian sent for signature)
into this folder. Any filename.

Claude reads it from this branch and diagnoses why the embedded fonts are
missing. This branch is not deployed — nothing here reaches production.

Delete the branch once the bug is fixed.
